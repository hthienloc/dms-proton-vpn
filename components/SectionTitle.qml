import QtQuick
import qs.Common
import qs.Widgets

StyledText {
    id: root
    width: parent.width
    text: ""
    font.pixelSize: Theme.fontSizeMedium
    font.weight: Font.Medium
    color: Theme.surfaceText
}
